# Development environment (Docker).
# Used when running the app inside docker-compose (dev profile).
# Values are placeholders — safe to commit.

NODE_ENV=development
LOG_LEVEL=debug
DATABASE_URL="postgresql://inventory:inventory_secret@postgres:5432/inventorystock"
JWT_SECRET="dev-only-not-for-production"
SUPPLIER_API_URL="https://dummyjson.com"
PORT=8000
FRONTEND_URL="http://localhost:9000"
